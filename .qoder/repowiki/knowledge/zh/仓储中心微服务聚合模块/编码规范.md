- API模块中DTO类名严格遵循 `Request`/`Response` 或 `DTO`/`VO` 后缀规范，区分请求与响应模型。
- Provider模块中数据访问层统一采用 `Mapper` 接口配合 XML 映射文件，实体类以 `PO` 或 `DO` 结尾。
- 跨模块通信接口在API层定义为 `*Client` 接口，并在Provider层通过 `*RpcController` 进行实现暴露。