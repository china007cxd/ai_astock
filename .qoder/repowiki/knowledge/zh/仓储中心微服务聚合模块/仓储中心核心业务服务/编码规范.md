- RPC接口统一返回`BaseResponseDto`包装结果，并通过`RPCResponseDtoUtil`工具类标准化异常处理与响应构建。
- 业务对象转换遵循Form/DTO -> PO -> VO的分层映射规范，在`converter`包下按业务域组织转换器组件。
- 关键业务操作（如一键入库、出库扣减）通过自定义注解`@SysLog`结合AOP切面实现操作日志的自动采集与持久化。
- 枚举类按业务模块（stockin, stockout, warehouse等）在`common/enums`下进行精细化分类管理，确保状态定义的统一性。