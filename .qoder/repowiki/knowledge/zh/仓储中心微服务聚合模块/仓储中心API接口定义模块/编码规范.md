- DTO类名严格遵循业务场景后缀规范，如 `Request`/`Form` 表示入参，`Response`/`VO`/`DTO` 表示出参。
- 枚举类统一提供 `getByCode` 或 `getNameByCode` 等静态工具方法，支持根据编码反向查找枚举实例或名称。
- 复杂业务域的DTO采用二级子包结构（如 `instockcheck/req` 与 `instockcheck/res`）隔离查询条件与返回结果。